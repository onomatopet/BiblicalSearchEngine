<Window x:Class="BiblicalSearchEngine.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Biblical Search Engine - Outil d'Étude Biblique" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu Principal -->
        <Menu Grid.Row="0">
            <MenuItem Header="Fichier">
                <MenuItem Header="Importer Document..." Click="ImportDocument_Click"/>
                <MenuItem Header="Importer Bible..." Click="ImportBible_Click"/>
                <Separator/>
                <MenuItem Header="Quitter" Click="Quit_Click"/>
            </MenuItem>
            <MenuItem Header="Outils">
                <MenuItem Header="Réindexer" Click="Reindex_Click"/>
                <MenuItem Header="Paramètres" Click="Settings_Click"/>
            </MenuItem>
        </Menu>

        <!-- Barre de Recherche -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox" 
                     Grid.Column="0" 
                     Height="35"
                     VerticalContentAlignment="Center"
                     FontSize="14"
                     Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                     KeyDown="SearchBox_KeyDown">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                </TextBox.InputBindings>
            </TextBox>

            <Button Grid.Column="1" 
                    Content="Rechercher" 
                    Width="100" 
                    Height="35"
                    Margin="5,0"
                    Command="{Binding SearchCommand}"/>

            <Button Grid.Column="2" 
                    Content="Avancé" 
                    Width="80" 
                    Height="35"
                    Click="AdvancedSearch_Click"/>
        </Grid>

        <!-- Zone Principale -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Panneau Latéral -->
            <Border Grid.Column="0" BorderBrush="LightGray" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" 
                               Text="Documents" 
                               FontWeight="Bold" 
                               Margin="10" 
                               FontSize="16"/>

                    <TreeView Grid.Row="1" 
                              x:Name="DocumentTree"
                              ItemsSource="{Binding Documents}">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <TextBlock Text="{Binding Name}" />
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </Grid>
            </Border>

            <!-- Zone de Résultats -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                           Margin="10"
                           FontSize="14"
                           Text="{Binding ResultsInfo}"/>

                <ListBox Grid.Row="1" 
                         x:Name="ResultsList"
                         ItemsSource="{Binding SearchResults}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="LightGray" 
                                    BorderThickness="0,0,0,1" 
                                    Padding="10"
                                    MouseLeftButtonUp="Result_Click">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" 
                                               Text="{Binding Title}" 
                                               FontWeight="Bold" 
                                               FontSize="14"/>

                                    <TextBlock Grid.Row="1" 
                                               Text="{Binding Reference}" 
                                               FontStyle="Italic" 
                                               Foreground="Gray"/>

                                    <TextBlock Grid.Row="2" 
                                               Text="{Binding Preview}" 
                                               TextWrapping="Wrap" 
                                               Margin="0,5,0,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Grid>

        <!-- Barre de Statut -->
        <StatusBar Grid.Row="3">
            <StatusBarItem Content="{Binding StatusMessage}"/>
            <StatusBarItem HorizontalAlignment="Right" 
                           Content="{Binding IndexInfo}"/>
        </StatusBar>
    </Grid>
</Window>